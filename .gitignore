build/
*.o
*.elf
*.ppm
*.img
*.img.gz
launcher/bin/
launcher/obj/
launcher/publish/

# Per-run logs from the pipeline. Kept on disk so a blocked task can be
# looked at, but they are large and belong to one machine.
pipeline/state/
